
const params=new URLSearchParams(location.search);const key=params.get('cat')||'banks';const cat=window.APP_DIRECTORY[key]||window.APP_DIRECTORY.banks;document.title=cat.title+' | iStoreNavid';document.getElementById('catTitle').textContent=cat.title;document.getElementById('catEmoji').textContent=cat.emoji;const grid=document.getElementById('appsGrid');cat.apps.forEach((app,i)=>{const [name,desc,url]=app;const card=document.createElement('article');card.className='app-card';card.innerHTML=`<img src="assets/apps/${key}-${i+1}.png" alt="لوگوی ${name}"><div><h2>${name}</h2><p>${desc}</p><a class="open-app" href="${url}" target="_blank" rel="noopener">باز کردن نسخه وب</a><button class="guide-mini" type="button">راهنما</button></div>`;card.querySelector('.guide-mini').onclick=()=>alert('۱) وب‌اپ را در Safari باز کنید.\n۲) دکمه Share را بزنید.\n۳) Add to Home Screen را انتخاب کنید.\n۴) روی Add بزنید.');grid.appendChild(card)});
